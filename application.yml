spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zhice_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Singapore
    username: root
    password: lusini123
    driver-class-name: com.mysql.cj.jdbc.Driver
  ai:
    deepseek:
      # 请替换为你自己的 API Key (如果是国内模型代理，填国内的Key)
      api-key: sk-711e0c82b50343d5928facd78d62e08f
      # 默认是 https://api.openai.com，如果是国内模型(如DeepSeek/通义千问代理)，请修改此处
      base-url: https://api.deepseek.com
      embedding:
        options:
          # 使用向量化模型
          model: text-embedding-3-small
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true # 开启驼峰命名转换
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 开发环境打印 SQL
  global-config:
    db-config:
      id-type: auto # 默认自增 ID，可根据需要改成雪花算法 (assign_id)
      logic-delete-field: deleted # 逻辑删除字段


# 在原有配置下方追加
